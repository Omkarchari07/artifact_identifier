[1mdiff --git a/app.py b/app.py[m
[1mindex 93e8912..b2316a0 100644[m
[1m--- a/app.py[m
[1m+++ b/app.py[m
[36m@@ -2,6 +2,8 @@[m [mfrom flask import Flask, request, jsonify, send_from_directory[m
 from flask_cors import CORS[m
 [m
 import os[m
[32m+[m[32mimport logging[m
[32m+[m[32mimport time[m
 from PIL import Image[m
 import numpy as np[m
 import tensorflow as tf[m
[36m@@ -10,6 +12,7 @@[m [mfrom pathlib import Path[m
 [m
 app = Flask(__name__)[m
 CORS(app)[m
[32m+[m[32mapp.logger.setLevel(logging.INFO)[m
 [m
 # =========================[m
 # LOAD MODEL[m
[36m@@ -22,7 +25,10 @@[m [mLABELS_PATH = MODEL_DIR / "labels.json"[m
 ROOT_LABELS_PATH = BASE_DIR / "labels.json"[m
 ARTIFACT_METADATA_DIR = BASE_DIR / "dataset_split" / "val"[m
 [m
[32m+[m[32mapp.logger.info("Loading TensorFlow model from %s", MODEL_PATH)[m
[32m+[m[32m_model_load_start = time.perf_counter()[m
 model = tf.keras.models.load_model(MODEL_PATH)[m
[32m+[m[32mapp.logger.info("TensorFlow model loaded in %.2f seconds", time.perf_counter() - _model_load_start)[m
 [m
 # =========================[m
 # LOAD LABELS[m
[36m@@ -64,6 +70,7 @@[m [mdef load_artifact_database(metadata_root):[m
 [m
 [m
 ARTIFACT_DATABASE = load_artifact_database(ARTIFACT_METADATA_DIR)[m
[32m+[m[32mapp.logger.info("Loaded %d artifact metadata records from %s", len(ARTIFACT_DATABASE), ARTIFACT_METADATA_DIR)[m
 [m
 ARTIFACT_INDEX = {}[m
 [m
[36m@@ -141,16 +148,28 @@[m [mdef lookup_artifact_info(class_name):[m
 @app.route("/predict", methods=["POST"])[m
 def predict():[m
 [m
[32m+[m[32m    request_start = time.perf_counter()[m
[32m+[m[32m    app.logger.info("/predict request received")[m
[32m+[m
     if "image" not in request.files:[m
[32m+[m[32m        app.logger.warning("/predict request missing image field")[m
         return jsonify({"error": "No image uploaded"}), 400[m
 [m
     file = request.files["image"][m
 [m
[32m+[m[32m    app.logger.info("Received upload filename=%s content_type=%s", file.filename, file.content_type)[m
[32m+[m
[32m+[m[32m    preprocess_start = time.perf_counter()[m
[32m+[m
     img = Image.open(file.stream).convert("RGB")[m
 [m
     img = prepare_image(img)[m
 [m
[32m+[m[32m    app.logger.info("Image preprocessing completed in %.2f seconds", time.perf_counter() - preprocess_start)[m
[32m+[m
[32m+[m[32m    predict_start = time.perf_counter()[m
     preds = model.predict(img)[0][m
[32m+[m[32m    app.logger.info("model.predict() completed in %.2f seconds", time.perf_counter() - predict_start)[m
 [m
     top_idx = int(np.argmax(preds))[m
 [m
[36m@@ -173,6 +192,8 @@[m [mdef predict():[m
             "artifact_info": lookup_artifact_info(class_name)[m
         })[m
 [m
[32m+[m[32m    app.logger.info("Response generation completed in %.2f seconds", time.perf_counter() - request_start)[m
[32m+[m
     return jsonify({[m
         "top1": {[m
             "class": top_class,[m
